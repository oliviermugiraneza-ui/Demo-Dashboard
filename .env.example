# Azure Databricks Lakebase PostgreSQL connection
# Replace <YOUR_OAUTH_TOKEN> with a token obtained from the Databricks console.
# Tokens expire every hour — update this value and the server auto-retries on the next request.
DATABASE_URL=postgresql://olivier.mugiraneza%40wayve.ai:<YOUR_OAUTH_TOKEN>@ep-soft-waterfall-e9retxot.database.eastus.azuredatabricks.net/databricks_postgres?sslmode=require

# API server port (leave at 3001 unless you have a conflict)
API_PORT=3001
